#include <stdio.h>
int main()
{
    double t,v,sum;
    printf("Enter the value of t : ");
    scanf("%lf", &t);
    printf("Enter the value of v : ");
    scanf("%lf", &v);
    sum =  v*2*t;
    printf("Samation is : %lf\n", sum);
    return 0;
}
